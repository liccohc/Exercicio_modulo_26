//funcao para calcular uma area
function calcularArea (base: number, altura: number): number {
    const area = base * altura;
    return area;
};
const resultado = calcularArea(168, 2.37);
console.log(resultado);